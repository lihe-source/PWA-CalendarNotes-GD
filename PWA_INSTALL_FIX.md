# 行事曆・備註 V2.2.1 — PWA 獨立安裝修正

## 部署

1. 確認現有資料已同步，備份重要本機資料；記下連線設定。
2. 解壓 ZIP，將同名資料夾「裡面的所有檔案」上傳至原 GitHub 儲存庫根目錄，覆蓋同名檔案。不要把外層版本資料夾上傳成子目錄。
3. 等候 GitHub Pages 部署成功，再從瀏覽器開啟 https://lihe-source.github.io/PWA-CalendarNotes-GD/ 。
4. 到設定頁檢查更新，確認目前版本為 V2.2.1。啟動時也會自動檢查；編輯或傳輸進行中會延後切換以保護資料。

本版修正只需更新 GitHub Pages 前端；已正常運作的 Cloudflare Worker 不必重新部署，也不需要執行 D1 初始化或資料庫遷移。完整原始碼仍保留 Worker 與既有部署文件。

若上傳前修改 config.js 的部署設定，必須在專案資料夾執行 `node release.mjs`，再把產生的 version.json 與其他更新檔案一起上傳，否則資產雜湊不符會阻止更新。

## 已安裝的電腦版：一次性重建

此次修正會改變 App ID；程式更新不會保證自動修復 Windows 的舊捷徑或工作列群組。

1. 先完成兩個網站的新版部署與資料備份，關閉兩個 App。
2. 在原安裝瀏覽器的應用程式管理頁移除混用的舊 PWA（Chrome：chrome://apps；Edge：edge://apps）。若顯示「清除網站資料」等選項，請不要勾選。
3. 取消舊工作列釘選，刪除失效的桌面捷徑。不要清除整個 lihe-source.github.io 網域的網站資料，以免影響其他 PWA。
4. 使用同一瀏覽器及使用者設定檔，分別開啟兩個正式網址，透過網址列的安裝功能重新安裝。
5. 分別啟動、確認名稱與內容，重新釘選到工作列。勿複製同一個舊捷徑再更名。
6. 檢查登入與同步。行事曆若通知訂閱需要恢復，請在設定頁重新啟用並測試通知。

## 固定識別

- 行事曆：`/PWA-CalendarNotes-GD/`
- 雲匣：`/PWA-Cloud/`

App ID 不加入版本號；未來升版保持相同。啟動網址與作用範圍固定在各自 GitHub Pages 專案目錄。本包以原本的儲存庫名稱部署；若搬到不同路徑或網域，需另行規劃識別遷移。

## 部署後驗收

- 設定頁顯示正確目前版本與最新版本，檢查更新可操作。
- Chrome DevTools → Application → Manifest 顯示兩個不同 App ID；也可由 chrome://web-app-internals 檢查 manifest_id。
- 兩個 App 可同時啟動，在 Windows 工作列各自歸類，捷徑各自開啟正確頁面。
- 分別重新開啟及離線開啟；更新一個不刪除另一個快取。
- 確認資料、Google 登入及 Drive 同步；行事曆通知點擊後開啟行事曆。

此交付已執行原始碼及發布檔案檢查；使用者電腦上的 Windows 安裝整合、正式帳戶授權與通知仍須部署後實測。

## 本版交付檢查結果

- 全部 JavaScript / MJS 語法檢查通過。
- 兩個 Manifest 識別不同，啟動與作用路徑符合原 GitHub Pages 專案。
- Manifest 圖示、捷徑與 HTML 本機資產路徑檢查通過。
- 版本設定一致；行事曆 16 項資產雜湊檢查通過。
- 原有追蹤檔案完整保留；ZIP 不包含 Git 內部資料。
- 未在使用者 Windows 電腦測試工作列、登入或正式通知。
