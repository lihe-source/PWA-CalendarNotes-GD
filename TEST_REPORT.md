# V2.0.0 測試報告

本報告針對本 ZIP 的原始碼。測試使用本機 SQLite、IndexedDB 模擬、DOM 模擬及模擬雲端回應；並未修改正式資料庫或部署網站。

## 已通過

| 範圍 | 驗證內容 |
| --- | --- |
| 資料庫／API | 重複執行升級、行程與備註 CRUD、重送冪等性、版本衝突、並行修訂防護、增量游標、唯讀成員權限 |
| 還原 | 中途 SQL 失敗整批回滾、成功還原、預覽過期的衝突拒絕 |
| 離線與同步 | 網路失敗保留待同步、上傳中再次編輯不遺失、本機未同步內容不被遠端覆蓋、帳號隔離、衝突副本 |
| 重複日期 | 月底跳過無效日期、閏年、夏令時間無效時間、跨日與全天結束界線 |
| 提醒 | 供應商失敗後重試、成功後不重送、已刪除來源不推送 |
| 授權 | 模擬 Google code exchange、權杖加密、會話雜湊、並行更新合併、登出失效、來源檢查 |
| DOM 操作 | 啟動、42 格月曆、24 小時選項、新增行程與備註、完成篩選、主題切換、模擬登入與工作區同步、本機內容匯入 |
| PWA 更新 | 資源雜湊驗證、受控啟用、離線 JS、遺失 JS 不回傳 HTML、通知聚焦同一 App、拒絕不完整版本 |
| 靜態完整性 | 所有 JS／MJS 語法、16 個前端資源雜湊、相對模組引用、版本一致性、GitHub Pages 必要標記 |

測試中刻意觸發的 500／409／403 是失敗回復與權限測試的一部分，均符合預期。Wrangler dry-run 已產生 Worker bundle 並列出預期綁定；程序後續網路請求受到環境限制，未據此宣稱遠端部署成功。

## 部署後仍需確認

- 真實 Google OAuth 授權與 refresh token、Drive 權限、附件上傳／續傳及備份下載。
- Cloudflare 正式 D1 升級與 Cron 排程；本機 SQLite 通過不代表已執行遠端升級。
- iPhone／iPad／Chrome／Edge 實機版面、鍵盤安全區、PWA 安裝與作業系統通知投遞。本次沒有執行真實瀏覽器或實機端到端測試。
- 推播供應商接受請求不等於裝置已顯示通知；依 DEPLOY.md 的通知確認步驟驗收。

持續登入需要 DEPLOY.md 所列 Worker secrets。未設定時使用相容登入模式，不能保證免重新登入。
