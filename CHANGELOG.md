# V2.2.2

啟動與切回行事曆選取今天；行程／備註列表獨立捲動；修正橫向整頁移動。詳見 UPDATE_V2_2_2.txt。

# 更新紀錄

## V2.2.0（2026-09-08）

- 修正電腦版有效登入期間仍顯示「重新授權」按鈕的錯誤。
- 啟動時若只剩已到期的短效權杖，使用現有 Google 工作階段執行 `prompt: none` 無提示恢復。
- App 開啟期間權杖到期時，由 API 共用更新流程自動換發，使用者不需中斷操作。
- Worker 長效會話失效時先嘗試瀏覽器無提示登入；暫時失敗採冷卻後重試，避免登入迴圈。
- 設定頁清楚區分伺服器長效登入與瀏覽器工作階段自動登入。

## V2.1.0（2026-09-08）

- 開啟 App 後在背景驗證既有會話，自動更新 Google 權杖並連接原工作區。
- 登入狀態改為「登入中／自動登入／已連線／需授權」，避免把舊帳號名稱誤判為有效登入。
- 暫時性網路或 Worker 錯誤不刪除會話，網路恢復或回到 App 時自動重試。
- Worker 會話採 30 天閒置續期與 180 天最長期限；refresh token 仍以 AES-GCM 加密保存。
- 設定讀取失敗不再永久退回短效登入模式；設定頁顯示自動登入是否完成部署。

## V2.0.0

- 依確認設計重製 UI／icon。
- 離線佇列、帳號隔離、序號同步、條件寫入與重送去重。
- 原子還原、復原快照、附件續傳、Google 伺服器授權更新。
- 共享日期規則、裝置推播重試、版本完整性校驗。

## 舊版更新紀錄（保留原文）


### UPDATE_V1_1_1

CalendarNotesPWA V1.1.1 更新說明

本次修改：
1. 新增行程時，「提醒」預設勾選「準時」。
2. 新增行程時，「開始時間」預設為使用者開啟新增行程視窗當下的日期與時間。
3. 「結束時間」預設自動設定為開始時間 + 1 小時。
4. 新增行程視窗內，在使用者尚未手動調整結束時間之前，修改開始時間會自動重新計算結束時間為 +1 小時。
5. 若使用者手動修改結束時間，之後不再強制覆寫，保留使用者自行設定的結束時間。

部署：
- 本次只有 GitHub Pages 前端更新，不需重新建立 D1、不需執行 schema.sql，也不需重新部署 Cloudflare Worker。
- 將 V1.1.1 ZIP 內檔案覆蓋 GitHub Pages repository 根目錄即可。


### UPDATE_V1_2_0

CalendarNotesPWA V1.2.0 更新內容
================================

1. Google 帳號記住登入
- 將有效的短效 Google access token 與最近使用帳號資訊保存於本機 IndexedDB。
- PWA 關閉後重新開啟，若 token 仍有效會直接恢復。
- token 到期時，若已有記住帳號，啟動時自動使用 Google GIS prompt=none 嘗試無畫面重新授權。
- Google 因安全政策要求重新驗證/同意時，才需要再次按「登入 / 授權 Google Drive」。
- 按「登出」會撤銷目前 token 並清除本機記住帳號。

2. iPhone / 手機新增行程視窗修正
- Dialog 本體固定，不再被內容拖動或水平位移。
- 僅中間欄位區可上下捲動。
- Header 與儲存/取消按鈕固定在視窗內。
- datetime-local、附件、提醒等欄位限制在視窗寬度內，避免超出右側。

3. 月曆跨月日期行為
- 點擊月曆內「下個月/上個月的灰色日期」時，只選取該日期，不再自動切月並重排畫面。
- 因此原本上一週仍保留在同一視圖。
- 要切換月份請使用左右箭頭或手機左右滑動。

4. WK 週別
- 月曆左側新增 ISO 8601 週別欄。
- 例如 2026/08/27 顯示在 WK35。
- 選取日期所在的 WK 會高亮。

5. 日期位置
- 每個日期數字使用 absolute positioning 固定在日期格左上角。
- 手機及桌面均不再受到 button 內容排版影響而跑回中央。

6. Push 通知標題
- Cloudflare Worker 在通知送出前會重新讀取事件/備註目前標題。
- 行程通知的 Notification title 直接使用「事件標題」。
- 內容則顯示時間、地點或說明。
- 即使 D1 中存在舊 reminder 資料，也會優先使用目前事件標題。

部署注意：
- 前端檔案需全部覆蓋 GitHub Pages。
- 因 worker.js 有修改，本版需要重新執行：npx wrangler deploy
- D1 schema 沒有修改，不需要重新執行 schema.sql。
- VAPID / Google OAuth Client ID / D1 database_id 不需重建。


### UPDATE_V1_3_0

CalendarNotesPWA V1.3.0

本次更新：
1. 月曆日期格內的國定假日與使用者事件改為固定由上往下排列，不再垂直置中。
2. 設定 > 介面外觀新增「介面風格」下拉選單：
   - Windows 10 風格
   - Mac 風格
   - iOS 26 風格
   - 卡通風格（偏黃綠色）
3. 介面風格使用 localStorage + IndexedDB 保存，重新開啟 PWA 會自動套用。
4. 原有黑色／白色介面切換保留，能與四種介面風格搭配。
5. 本版只修改 GitHub Pages 前端，不需重新部署 Cloudflare Worker，也不需修改 D1 Schema。


### UPDATE_V1_4_0

CalendarNotesPWA V1.4.0 更新內容

1. 介面主題
- 移除 Windows 10、Mac、iOS 26。
- 保留卡通造型並增加為 5 款配色：黃綠草地、天空藍、蜜桃橘、薰衣草紫、莓果粉。
- 舊版 windows10/mac/ios26/cartoon 選擇會自動遷移到「卡通・黃綠草地」。
- 選擇以 localStorage + IndexedDB 保存。

2. 手機月曆當日事項
- 將當日事項區調整為約 26dvh，並以 flex 分配可用空間。
- 事件清單本身可捲動，但不再只有約 66px 高度。
- 說明可顯示最多兩行，閱讀性提高。

3. iOS 懸浮視窗關閉後排版
- 新增統一 openAppDialog/closeAppDialog。
- 關閉時先 blur 鍵盤焦點，清除 modal lock，並以兩階段 viewport scroll 校正避免頁面底部異常空白。

4. Google 帳號顯示
- 登入後顯示 Google 頭像、使用者名稱、Email 與「已登入」標籤。
- 已登入時隱藏「登入 / 授權 Google Drive」按鈕，只保留登出。
- 權杖到期但帳號仍記住時，按鈕改顯示「重新授權 Google Drive」。

部署：本版只有 GitHub Pages 前端變更，不需重新部署 Cloudflare Worker，不需重建 D1。


### UPDATE_V1_5_0

CalendarNotesPWA V1.5.0 — 多人共享工作區

【核心變更】
1. V1.4.x 以前：Cloudflare D1 依 Google user_sub 隔離，每位 Google 使用者看到自己的行程與備註。
2. V1.5.0：新增 shared-main 共享工作區。已加入同一工作區的 Google 使用者共同讀寫同一份：
   - 行事曆
   - 備註 / 待辦
   - Google Drive 附件
   - 共享設定（Drive Folder、時區）
   - Reminder Engine
3. 事件提醒會推播到共享工作區內「已啟用通知」的所有成員裝置。
4. Google Drive 共用資料夾同時作為加入資格驗證：
   - 使用者必須能以自己的 Google 帳號存取該 Folder ID。
   - 編輯者可共同新增 / 修改 / 上傳附件。
   - Viewer 權限會以唯讀成員加入。
5. 第一個共享工作區固定綁定一個 Drive Folder ID；後續成員必須使用同一 Folder ID。
6. 設定頁新增共享工作區狀態與成員清單。

【既有 V1.4.0 升級：必做】
A. 先備份目前 D1（建議）。
B. 在 V1.5.0 資料夾執行一次：
   npx wrangler d1 execute calendar-notes-pwa-db --remote --file=./migrate_v1_5_0.sql
C. 再部署 Worker：
   npx wrangler deploy
D. 將 V1.5.0 所有前端檔案覆蓋 GitHub Pages repository 根目錄。
E. 開啟 PWA，確認版本 V1.5.0，登入原本 Google 帳號並同步。

注意：migrate_v1_5_0.sql 是一次性 ALTER TABLE migration；既有資料庫只執行一次。
全新建立的 D1 請直接執行 schema.sql，不要再執行 migrate_v1_5_0.sql。

【讓第二位使用者加入】
1. Google Drive 網頁將「同一個共用資料夾」分享給第二位 Google 帳號。
2. 若要共同編輯 / 上傳，Drive 權限選「編輯者」。
3. Google Cloud OAuth 若仍為 Testing，將第二位帳號加入 OAuth Test users。
4. 第二位使用者開啟 PWA → Google 登入。Worker 會自動測試這個帳號是否有共享 Folder 權限；有權限就自動加入，不需要再貼 Folder ID。
5. 若自動加入失敗，才到「Google Drive 共用資料夾」貼上與第一位完全相同的 URL / Folder ID，按「加入 / 測試並儲存」。
6. 成功後，兩人會看到同一份行程、備註與成員清單。
7. 每一台需要接收提醒的裝置，各自按一次「啟用通知」。


### UPDATE_V1_6_0

CalendarNotesPWA V1.6.0 更新內容

1. 響應式裝置版面
- 手機 <=767px：底部導航、固定月曆工作區、觸控優先。
- 平板 768–1099px：寬版雙欄內容、置中導航。
- 桌機 >=1100px：左側常駐導航、月曆與當日事項左右並排、設定頁三欄。
- 使用 CSS viewport breakpoint 自動切換，不依 User-Agent，視窗縮放與橫直向切換皆會重新適配。

2. 新增 5 款卡通主題（總共 10 款）
- 薄荷汽水
- 檸檬奶油
- 珊瑚夕陽
- 焦糖可可
- 星空靛藍
原本 5 款黃綠草地、天空藍、蜜桃橘、薰衣草紫、莓果粉全部保留。
所有主題仍可搭配黑色/白色模式，並保存使用者選擇。

3. 部署
- 本次僅修改前端介面與版本快取。
- 不需重新執行 D1 migration/schema。
- 不需重新部署 Cloudflare Worker。
- 只需將 ZIP 內檔案覆蓋到 GitHub Pages repository 根目錄。


### UPDATE_V1_6_1

CalendarNotesPWA V1.6.1 更新內容

1. 所有時間選擇改為強制 24 小時制，不再依 iOS / Windows / Chrome 地區設定顯示 AM/PM。
2. 開始時間、結束時間改為「日期 + 時(00-23) + 分(00-59)」選擇器。
3. 備註提醒時間同步改為 24 小時制；未設定日期時不建立提醒。
4. 新增行程仍以當下日期時間為開始，結束預設 +1 小時；修改開始時間時仍會自動更新結束時間。
5. 月曆/備註中顯示的時間同步固定為 24 小時制。
6. 此版僅修改前端，不需重新部署 Cloudflare Worker，也不需執行 D1 migration。


### UPDATE_V1_6_2

CalendarNotesPWA V1.6.2

修正項目：
1. 修正手機版新增/編輯行程視窗內的日期、時、分選擇框排版與大小。
2. 修正 24 小時制時間欄位在小螢幕上會超出或重疊的問題。
3. 調整月曆頁下方「當日事項」區塊高度，減少空白並保留內部捲動。
4. 微調對話框內容與按鈕區留白，避免視覺上出現過多底部空白。


### UPDATE_V1_6_3

CalendarNotesPWA V1.6.3

修正項目：
1. 再次優化 iPhone 手機版新增/編輯行程視窗排版。
2. 縮小標題、欄位與底部按鈕區高度，讓更多內容能在同一畫面內顯示。
3. 修正日期/時/分 24 小時制欄位在手機上的大小與間距。
4. 讓對話框內容區成為更穩定的可捲動區塊，避免底部欄位被擠壓。
5. 調整月曆頁下方「當日事項」區塊高度，減少空白過大問題。


### UPDATE_V1_6_4

CalendarNotesPWA V1.6.4

修正項目：
1. 進一步壓縮 iPhone 手機版新增/編輯行程視窗的標題列、欄位高度與按鈕列高度。
2. 再縮小日期、時、分選擇器的尺寸與間距，讓更多欄位同時出現在畫面中。
3. 再縮小月曆頁下方「當日事項」區塊高度，降低過多留白。
4. 微調 checkbox 與按鈕尺寸，提升小螢幕閱讀與點擊平衡。


### UPDATE_V1_6_5

CalendarNotesPWA V1.6.5

修正項目：
1. 修正 iPhone 手機版新增 / 編輯行程視窗中，開始時間與結束時間欄位右側會超出畫面的問題。
2. 強制限制日期、時、分欄位在小螢幕上為 100% 內寬，不再溢出對話框。
3. 調整手機版內容區左右內距與時分欄位間距。


### UPDATE_V1_6_6

CalendarNotesPWA V1.6.6

修正項目：
1. 重新設計新增／編輯行程的 24 小時日期時間欄位結構。
2. 修正 iPhone 上更新後開始時間／結束時間沒有可選欄位的問題。
3. 一併修正小螢幕下日期、時、分欄位溢出視窗的問題。
4. 保留開始時間=現在、結束時間=開始+1小時的既有邏輯。
